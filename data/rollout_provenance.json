{
  "_README": [
    "Which collected episodes must NOT be aggregated into reported metrics, and why.",
    "",
    "Read by src/slava_rollout/provenance.py, used by both report generators.",
    "This file is the single place that decides what counts as valid data, on",
    "purpose: it is version-controlled, reviewable in a diff, and identical on",
    "every machine. It replaced an mtime-based heuristic that gave different",
    "answers on different machines for the same dataset (see that module's",
    "docstring for the specific failure).",
    "",
    "Add an exclusion when episodes were collected with inference code that has",
    "since been found wrong, and the rerun has not happened yet. Each entry must",
    "name the bug, the evidence, and what would clear it. Remove the entry only",
    "after the affected episodes have actually been re-collected — not because",
    "the code was fixed.",
    "",
    "Selectors (all optional, combined with AND; at least one is required):",
    "  models       list of `model` display names, as written in the annotations",
    "  environment  \"LIBERO\" or \"SimplerEnv\"",
    "  variants     list of instruction-variant names",
    "  run_ids      explicit list of run_ids",
    "`short_reason` — нейтральная формулировка для отчёта (одна строка, по-русски). Развёрнутые `reason`/`evidence` остаются внутренней записью репозитория и в отчёт не идут."
  ],
  "exclusions": [
    {
      "id": "smolvla-simplerenv-action-space-mismatch",
      "models": [
        "SmolVLA"
      ],
      "environment": "SimplerEnv",
      "reason": "lerobot/smolvla_base is an SO-100 checkpoint, not a cross-embodiment base model. Its own config declares output_features['action'].shape == (6,) and its postprocessor ships unnormalization statistics keyed only to so100 / so100-red / so100-blue. SimplerEnv's WidowX/bridge controller takes 7 values (6 end-effector deltas + gripper) in an entirely different action space. Unlike pi0/pi0.5 -- whose declared 32-dim space IS a zero-padded universal one, so `action[:7]` is the documented exact inverse of training's pad_vector() -- there is no truncation or padding of a 6-dim SO-100 joint command that yields a valid WidowX command. Evaluating this pair zero-shot is not defined.",
      "evidence": "PreTrainedConfig.from_pretrained('lerobot/smolvla_base').output_features == {'action': shape=(6,)}; the postprocessor's UnnormalizerProcessorStep carries only so100/so100-red/so100-blue action statistics; SimplerEnv's base_controller asserts action.shape == (7,). The 15 episodes collected for this pair are 0/15 successful with 9 labelled no_action_or_timeout, which is the signature of a command that means nothing to the robot rather than of a policy that tried and failed.",
      "affects": "15 episodes (SmolVLA x SimplerEnv). SmolVLA's 99 LIBERO episodes are unaffected: HuggingFaceVLA/smolvla_libero is fine-tuned on that embodiment and declares the matching action space.",
      "clears_when": "never for this checkpoint -- it would need a SmolVLA fine-tuned on bridge/WidowX, which is a different model, not a rerun",
      "short_reason": "чекпойнт обучен на другом манипуляторе (SO-100) и не определён на WidowX/bridge: пара «модель × среда» не оценивается zero-shot",
      "note": "Строк с этой парой в текущих аннотациях нет вовсе: 13 папок кадров вынесены в rollouts/archive/smolvla_simplerenv_excluded/ (07.08.2026). Поэтому генераторы отчётов печатают «matched nothing» — это ожидаемо, правило остаётся как объявление, что пара не оценивается, а не как отложенный пересбор."
    },
    {
      "id": "ru-case-swap-old-success-criterion",
      "variants": [
        "ru_case_swap"
      ],
      "reason": "Эпизоды сняты с прежним критерием успеха: для ru_case_swap успех брался из предиката среды, который намеренно не переворачивается вместе с инструкцией. Поэтому success=True означал «модель проигнорировала перестановку ролей и выполнила ИСХОДНОЕ задание» — величина, противоположная тому, что зонд должен измерять. Решение пользователя 07.08.2026: критерий заменён на проверку перевёрнутой инструкции (auto_label._swapped_success), но уже собранные результаты не переписываются.",
      "evidence": "36 эпизодов ru_case_swap, 4 из них с success=True; в двух проверенных вручную человек отметил провал там, где авторазметка записала успех, с пояснением «робот сделал обычное действие, а не инвертированное».",
      "affects": "36 эпизодов ru_case_swap в пуле pilot_v0",
      "clears_when": "пересбор ru_case_swap новым кодом, либо офлайн-пересчёт из сохранённых object_poses в steps.jsonl — данные для этого есть, решение о пересчёте за пользователем",
      "short_reason": "снято по прежнему критерию успеха для ru_case_swap; зонд измерял обратное тому, что задумано"
    }
  ]
}
