# Журнал текущей работы

Живое состояние сессии: то, что дорого восстанавливать после компактификации
контекста. Правила ведения — skill `slava-long-sessions`. Итоги отсюда уезжают
в `AGENTS.md` (срез проекта), `docs/OPEN_ISSUES.md` (открытые вопросы) и скилы
(повторяемые правила); здесь остаётся только процесс.

Последнее обновление: 07.08.2026.

## 1. Что делаем сейчас

Настраиваем инфраструктуру длинных и автономных сессий: видимость лимитов,
правила расхода бюджета, состояние на диске. Параллельно пользователь вручную
валидирует 100 роллаутов в `data/label_review.html`.

## 2. Следующие действия

1. Дождаться от пользователя `label_review_verdicts.json`, прогнать
   `scripts/apply_label_review.py`, разобрать расхождения как баги `auto_label.py`.
2. Согласовать скоуп автономной смены (решено обсуждать ближе к делу).
3. По возвращении GPU — диагностика GreenVLA (R0/R1/R2), затем lerobot-семейство.

## 3. Решения пользователя этой сессии

- **Подача в отчёте:** OpenVLA-OFT — `primary`, GreenVLA-R1/R2 — `preliminary`,
  GreenVLA-R0/SmolVLA/π0/π0.5 — `excluded`. Причина: у исключённых английская
  база слишком низка, чтобы отличить языковой эффект от поломки пайплайна.
- **Повторы эпизодов не добираем.** Пилот проверяет работоспособность идеи, а
  не даёт финальный замер; шум уйдёт объёмом на большом наборе.
- **Критерий успеха берём из SimplerEnv как есть** — авторские числа считались
  им же, свой строгий критерий сделал бы сравнение невозможным. Наблюдение про
  «успех при зажатом объекте» сохранено для фазы реальных роботов.
- **Порядок отладки:** сначала GreenVLA, сразу за ней lerobot-семейство.
- **HF-датасет удалён** как устаревший; офсайт-копии данных больше нет.
- **Автономная работа:** коммитить и пушить самостоятельно.
- **Форма вопросов:** открытые решения задавать формой с вариантами, всегда.
- **Бюджет:** порог парковки 15%; в автономном режиме всегда дожидаться сброса
  окна и продолжать, а не заканчивать смену (пользователь оставляет ноутбук на
  пару суток и его не будет). Вести журнал стоимости единиц работы вместе с
  размером контекста — `docs/BUDGET_LOG.md`, пишется `scripts/budget_log.py`.

## 4. Границы

Не трогать замороженный пилот D1-D4 и тег `slava-pilot-v0`. Не редактировать
`task.md` — это контракт пользователя. Не начинать v0.1 (проекция 3D→2D,
pointing-зонд) и не масштабировать на полный набор сцен без отдельного решения.
Коммит и пуш — по просьбе, кроме автономных смен и хэндоффа.

## 5. Фоновые процессы

Нет. (Арендованный GPU-сервер отработал и погашен.)

## 6. Внешние ресурсы

Нет активных. Прошлый инстанс vast.ai (2×RTX 3090) отработал 07.08.2026;
результаты выгружены в `rollouts/final/harness_validation_greenvla/`.

## 7. Что пробовали и не сработало

- Предзаполнение вердиктов в дашборде без отдельного флага «проверено» —
  отвергнуто: заполненная карточка выглядит проверенной, и измерение точности
  разметчика теряет смысл. Решение: флаг `reviewed` по явному действию.
- Опрос фоновых прогонов в цикле короткими интервалами — дорого по ходам;
  правильный способ — одно фоновое ожидание с условием выхода.

## 8. Ждём от пользователя

- `label_review_verdicts.json` после ручной валидации.
- Скоуп автономной смены.
- Проверить, показывается ли статуслайн в вашем фронтенде: кэш
  `~/.claude/usage-cache.json` пуст, то есть процентов лимита я не вижу и
  ориентируюсь на токены из транскрипта.

## 9. Незакоммиченное

Нет.

## 10. Ключевые числа (сверены 07.08.2026)

- Пилот: 536 эпизодов, покрытие полное, исключений из метрик нет.
- Валидация стенда: проходит 1 модель из 7 (OpenVLA-OFT 15/16 против ~97%).
- GreenVLA на полном bridge-наборе: R0 0/22, R1 5/22, R2 5/22.
- Тестов в репозитории: 49, зелёные.
- Повтор 12 эпизодов на другом железе: совпало 9 из 12.

## 11. Бюджетный журнал

`docs/BUDGET_LOG.md` — по строке на завершённую единицу работы: потраченные
токены, число вызовов, **размер контекста на тот момент** и сколько заняло по
времени. Пишется `python3 scripts/budget_log.py --mark "что сделали"`.
Первая строка — вся эта сессия целиком, как отправная точка.
