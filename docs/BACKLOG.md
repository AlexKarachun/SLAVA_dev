# Бэклог задач

Заведён 07.08.2026 по разбору пожеланий пользователя, накопленных за время
ручной валидации роллаутов. Живёт до закрытия задач; текущее состояние работы —
[`WORKLOG.md`](WORKLOG.md), открытые научные вопросы — [`OPEN_ISSUES.md`](OPEN_ISSUES.md).

Статусы: `[ ]` не начата · `[~]` в работе · `[x]` сделана · `[?]` нужен ответ
пользователя.

## Данные, которые нельзя потерять

Вердикты ручной валидации: `data/label_review/verdicts_2026-08-07.json` —
57 явно подтверждённых эпизодов, 48 с текстовыми заметками, 24 расхождения с
авторазметчиком.

**Важно для расчёта точности (сказано пользователем 07.08.2026): пройдены все
100 карточек.** Там, где он не взаимодействовал с карточкой, он был согласен с
авторазметчиком и ему нечего было добавить. То есть 43 непроверенные карточки —
это молчаливое согласие, а не пропуск, и в знаменатель они входят.

Заметки пользователя описывают, что реально происходило на сцене, и это основной
материал для задач T5-T10.

---

## T1. Убрать упоминания научрука из репозитория

- [x] T1 **Вычёркивать нечего: упоминаний научрука в репозитории нет.**
      Поиск по `научн|руководител|куратор|препод|универ|диплом|сдач|supervisor|
      defence` дал только законные употребления («научная постановка»,
      «научный смысл») и одно слово «ревьюер» в значении «рецензент статьи».
      Единственное «Формат сдачи» — в `task.md`, который редактировать нельзя
      по вашему же правилу; если это про научрука, уберите там сами (строка
      211-213).

## T2. Пометить недостоверность моделей в их скилах

- [x] T2.1 Плашка добавлена сразу под фронтматтером.
- [x] T2.2 Плашка добавлена, с цифрами полного bridge-набора.
- [x] T2.3 Добавлено обратное: опорная модель для сравнений.

## T3. Актуализировать комментарии для агентов везде

- [ ] T3.1 Скилы `slava-*`: снять устаревшие утверждения (пути пулов, счётчики
      эпизодов, «покрытие неполное», старые исключения).
- [ ] T3.2 `AGENTS.md`, `README.md`, `docs/*.md` — сверить с фактическим
      состоянием.
- [ ] T3.3 Комментарии в коде `src/`, `scripts/` — особенно там, где менялись
      пути (`rollouts/final/...`) и контракты.

## T4. Большой разнообразный набор гипотез: почему 6 моделей не дают заявленный SR

- [x] Собрано в [`docs/HYPOTHESES.md`](HYPOTHESES.md): **27 гипотез** по
      источникам — наш инференс (A1-A10), наша среда и валидация (B1-B5),
      различия с авторами (C1-C4), чужие воспроизведения (D1-D2), здравый смысл
      и непопулярные версии (E1-E7). У каждой: что за и против в наших данных,
      чем проверяется, что предсказывает.
- [x] Порядок проверки по цене/информативности. **Первый кандидат — B1,
      горизонт эпизода: у GreenVLA 86-96% эпизодов упираются ровно в лимит
      SimplerEnv в 60 шагов, у π0 73% доходят до нашего потолка 300.** Проверка
      стоит полчаса GPU и одну переменную.
- [?] Нужен GPU для B1, A2, A7, D1. C1 (прочитать авторский eval целиком)
      можно делать без GPU — следующая задача, если пользователь не возразит.

## T5. Разбор заметок пользователя из валидации

- [x] T5.1 Таблица собрана — `docs/ROLLOUT_NOTES_ANALYSIS.md`.
- [x] T5.2 Почерки отказа по моделям различаются и записаны там же: π0
      «переворачивается», π0.5 «не докасывается», SmolVLA «тянется не к тому»,
      GreenVLA «недолёт и нерешительность», OpenVLA-OFT — претензии только к
      разметке.
- [x] T5.3 Сформулировано, что похоже на наш дефект, а что на поведение модели.
- [x] T5.4 Проверено и закрыто отрицательным результатом: разделить «замер» и
      «двигался мимо» по сохранённым логам **нельзя** — мы пишем команды
      политики, а не позу схвата; суммы несравнимы между средами и перекрываются
      внутри среды. Разбор в `ROLLOUT_NOTES_ANALYSIS.md`.
- [?] T5.6 Предложение пользователю: логировать позу схвата на каждом шаге в
      env-воркерах. Делает различие измеримым, но меняет контракт `steps.jsonl`
      и требует пересбора — решение за пользователем.
- [x] T5.5 Починено: если цель ни разу не поднялась выше 2 см, ставится
      `physical_execution_error`, а не `relation_binding_error` — нарушать
      отношение было нечем. Перемечено 56 эпизодов, 3 теста.
      **Согласие с человеком по метке: 76 → 87 → 92 из 100.**

## T6. Залогировать наши интерпретации меток провалов

- [x] T6.1 Записаны в `slava-model-rollouts`, раздел «Метки провалов
      человеческим языком», вместе с наблюдением, где чаще всего расходятся
      человек и автомат.
- [x] T6.2 Сверено с `task.md`: формулировки поясняют контракт, не меняя его.

## T7. Проверить обработку `ru_case_swap` в авторазметке

- [x] T7.1 Не меняются: во всех 8 промптах `ru_case_swap` target/reference
      совпадают с исходными. Это сделано намеренно — success-предикат тоже не
      меняется (зонд, а не задача).
- [x] T7.2 Проверено на двух эпизодах пользователя: робот выполнил ИСХОДНУЮ
      задачу, предикат сработал, авторазметка записала `success`. Человек
      пометил провалом.
- [x] T7.3 **Решение пользователя: критерий заменён на логичный.** Для
      `ru_case_swap` успех теперь означает «робот выполнил перевёрнутую
      инструкцию» и считается из финальных поз (`auto_label._swapped_success`,
      отношение `on`), а не из предиката среды. В строку аннотации добавлено
      поле `success_source` (`env` / `swapped_predicate`), чтобы единица в
      колонке `success` была читаема. Уже собранные 36 эпизодов **не
      переписаны**, а помечены устаревшими в `data/rollout_provenance.json` —
      в метриках теперь 500 из 536. Офлайн-пересчёт из сохранённых
      `object_poses` возможен, решение за пользователем.

## T8. Проверить условие `negation_error`

- [x] T8.1 Правило прочитано; реализация ставила метку по любому варианту.
- [x] T8.2 Подтверждено: из 21 negation_error лишь 4 были на `ru_negation`.
- [x] T8.3 **Прав пользователь.** Починено: метка только на оси отрицания,
      17 эпизодов перемечены в `target_grounding_error`, 3 теста, отчёт
      перегенерирован. Согласие с человеком по метке выросло 76 → 87 из 100.

## T9. Классификация «что нельзя определить кодом»

- [ ] T9.1 Дашборд провальных эпизодов для отметки «только по видео».
      Первый подтверждённый пункт списка уже есть: «замер» против «двигался
      мимо» (см. T5.4).
- [ ] T9.2 По отметкам — список признаков, которые в принципе не выводятся из
      логов, и что это значит для метрик.

## T10. Сверка меток с заметками

- [ ] T10.1 Пройти все 48 заметок и проверить, что метка соответствует описанию.
- [ ] T10.2 Спорные — вынести пользователю списком с предложением.

---

## Порядок

Сначала T7 и T8 (они меняют данные и могут потребовать перерасчёта), затем T5 и
T10 (разбор заметок, на их основе T4.5), потом T4 целиком, дальше T1-T3
(гигиена текста), T6 и T9.
